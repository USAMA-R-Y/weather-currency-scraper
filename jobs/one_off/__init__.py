from jobs.registry import OneOffJobSpec

# List of all one-off jobs
# Add one-off jobs here as needed
ONE_OFF_JOBS = [
    # Example:
    # OneOffJobSpec(
    #     module="jobs.one_off.example_job",
    #     func=example_job_function,
    #     job_id="example_job_v1",
    #     name="Example One-off Job",
    #     idempotency_key="example_job_v1"
    # ),
]
